next.json-generator.com

[
  
    {
    'repeat(100)': [
      '{{guid()}}',{
            
        attributes: {
   
          author: { S: '{{firstName()}} {{surname()}}'},
          text: {S: '{{lorem(1, "paragraphs")}}' },
          sentimentScore: {N: "{{floating(-1,1)}}" },         
          date: { S: '{{date(new Date(2017,0,1), new Date(2019,4,10), "YYYY-MM-ddTHH:mm")}}' },
          
          user: { S: function(tags) {
          return 'users' + tags.integer(0,10);
          }},
          keyword: {S:  function (tags) {
        var keywords = ['life', 'nike', 'javascript', 'microsoft', 'gameofthrones'];
        return keywords[tags.integer(0, keywords.length - 1)];
          }}
        
      } 
      }
      ]
    }
]
