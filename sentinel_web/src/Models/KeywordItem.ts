export class KeywordItem {
    keyword: string;
    isEditable: boolean;

    constructor(keyword: string, isEditable: boolean) {
        this.keyword = keyword;
        this.isEditable = isEditable;
    }
}
